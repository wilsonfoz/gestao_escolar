<%= simple_form_for(@aluno, :html => {:class => 'form-vertical'})  do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nome %>
    <%= f.association :turma %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
