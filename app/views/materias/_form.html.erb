<br />
<%= simple_form_for(@materia, :html => {:class => "form-horizontal"}) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nome %>
    <%= f.association :professores %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
